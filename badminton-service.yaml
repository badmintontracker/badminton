apiVersion: v1
kind: Service
metadata:
  name: badminton-tracker-lb
spec:
  type: LoadBalancer
  selector:
    app: badminton-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
